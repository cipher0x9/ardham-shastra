# Legacy v1 campus

The original single-file HTML campus (`university/v1-ARDHAM-SHASTRA.html`, ~12 MB) and its Python generators live here.

v2 is the Next.js campus at the repository root: typed mandalas, FSRS-6 reviews, Nyāya gates. Open `../` and run `pnpm dev`.

The HTML still opens offline in any browser if you need the old zip-shaped artifact.
